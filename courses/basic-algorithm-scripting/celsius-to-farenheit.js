/**
 * Convert temperature from Celsius to Fahrenheit.
 *
 * @param {number} celsius - Value of Celsius temperature in number.
 *
 * @return {number} The converted temperature to Fahrenheit.
 */
function convertToF(celsius) {
    return celsius*(9/5) + 32;
}

convertToF(30);
