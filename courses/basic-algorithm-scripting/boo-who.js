/**
 * Check if value is boolean primitive.
 *
 * @param {*} bool - The value to check if it is boolean primitive.
 *
 * @return {boolean} The check status.
 */
function booWho(bool) {
    return typeof bool === 'boolean';
}

booWho(null);
